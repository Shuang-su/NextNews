# MetaFlow 场景入口、声音与主体/环境（2026-09-16）

本批接续 `4917b8f`，仍为 PR #5 阶段实现，不代表完整 Viewer 对齐。

## 已实现

- MetaFlow 页面地址经 `/data/index.json` 解析 route/aliases、推荐 LOD、主体、环境、配置、封面和碰撞地址。显式查询参数覆盖索引值，资源按其配置来源解析；不改写原文件。
- 读取 `noanim/noreveal/nofx/noui/fullload/hpr/budget`，默认模式、figure8 与首次退出环绕接入现有控制层；默认步行等待碰撞加载。`nofx` 禁用配置中的效果，尚不意味着所有效果已实现。
- `viewer-settings.json` 支持注释与尾逗号，字符串中的 URL、逗号和注释符号不被误删。
- 背景音使用单场景 AVPlayer，手势及首次显示后播放，支持循环、静音/音量、前后台暂停恢复和释放；异步旧播放器不能覆盖新场景。
- 主体先显示、环境异步加入。OpenGL 合并同一选择与全局排序；环境计入预算，超限保留原画面并提示。华为单体路径添加同场景 GSNode。
- 合并环境保留主体相机归一化边界，独立扩大裁剪范围。揭示可扩展范围但保留已播放秒数，已结束开场不重播。未增加 LOD 加载遮罩。

## 验证

配套 SDK 构建、签名、安装启动成功。HUAWEI Mate 80 Pro Max（SGT-AL10，API 26，1320×2848）打开前海冰雪世界，首次加载可观察，六次往返转向没有重新显示加载界面。

本地证据：`artifacts/harmonyos/metaflow-scene-20260916/loaded.png`、`after-turns.png`、布局记录与 `result.json`。
调试包：`artifacts/harmonyos/NextNews-metaflow-scene.hap`。

通过的主机回归：配置相对地址/不可变性、JSONC、入口覆盖/推荐 LOD/异常地址、声音异步生命周期、动画组合、原生首帧交接、流式调度与环境范围/计数/预算保护；原生解析数学及揭示时间线经 ASan/UBSan。
主机测试不等于真机声音、双模型透明排序或原生华为渲染已验收。

## 剩余差异

- 手机声音播放、环境早到/晚到/失败/重试、多指和后台组合还需定向验证；环境缓存字节尚未纳入整场缓存统计。
- MetaFlow 地址入口目前接受 HTTPS 页面路径；不是 SZTV 的 hash 列表入口。后者继续使用既有 CDN 模型列表。
- `.json` 主体暂按 LOD 清单处理，单体 SOG 元数据地址分类仍需完善。
- 体素坐标配置已保留，但显式 `metaflow-rz180` 的完整接线未验收。
- 默认场景分类沿用归一化球边界；与参考 AABB 包含测试仍有差异。
- 后处理、天空盒、高阶 SH、碰撞完整矩阵与华为 tiled 等价性仍未完成；原性能门槛未重新测量，不能从这次六次转向回归认定 P95 达标。
