# OpenGL 标注深度默认启用：真机验证

用户反馈默认仍没有遮挡属实：旧包 gpuAnnotations=false，只有手动测试夹具启用 GPU 深度路径。此次完整跑通 verify_annotations.py 后改为 OpenGL 默认启用，保留设置开关及无深度附件时二维回退。

Mate80ProMax / SGT-AL10，API26，7.0.0.105(SP10C00E105R3P3)，实际深度缓冲 24 bit。测试截图人工检查确认：1 号前景保持清晰，2 号被不透明高斯覆盖后仅留淡轮廓，3 号半透明遮挡保留部分内容。按照官方 v1.31.2 annotation.ts 的底层深度写入、透明高斯、25% 叠加轮廓顺序，遮挡不是整颗标注消失，后景仍可点击。

脚本检查通过：后景点击正文/关闭、前后导航、镜头后方隐藏、屏幕外浮窗收边、24 bit 能力、Huawei 二维回退、前后台、切回 OpenGL 上下文重建及工具隐藏/恢复。重建截图仍具有遮挡。Huawei 没有等价遮挡，保持明确回退状态。

默认开启的最终签名包已重新安装。直接从列表加载正式 CDN 前海冰雪世界，未手动启用实验开关，显示 2M 高斯/10 标注；cdn-default-depth.png 可见场景表面对标注下半部分的逐像素遮挡，1 号前景保持清晰。

源码只改变默认和菜单标签，既有 GPU 绘制路径没有改写。项目构建、签名构建、标注投影/浮窗与触点测试通过。包 artifacts/harmonyos/NextNews-depth-preview.hap，SHA256 062c88d4f592c278495891076c6810249464e7a699e699d1f8e381e06055d0ae。

证据在 evidence/depth-20260916。未将这次功能测试计作官方 Web 逐像素一致或 20 次性能通过；横屏重叠、高斯中心深度近似、字体差异及 Huawei 深度路径仍待继续验证。
